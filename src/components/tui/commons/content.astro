---
import { cn } from "@/lib/cn";

interface Props {
  class?: string;
}
const { class: className } = Astro.props as Props;
---

<div class={cn("flex w-full flex-col gap-0.5 p-3", className)}>
  <slot />
</div>
